<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartattendancepro">

    <!-- Permissions -->

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartAttendancePro">

        <activity android:name=".activities.Home"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity"/>
        <activity android:name=".activities.RegisterActivity"/>
        <activity android:name=".activities.TeacherDashboardActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".activities.AttendanceActivity"/>
        <activity android:name=".activities.ViewDataActivity"/>
        <activity android:name=".activities.AllAttendanceActivity"/>
        <activity android:name=".activities.TeacherProfileActivity"/>
        <activity android:name=".activities.EditProfileActivity"/>
        <activity android:name=".activities.ReportActivity"/>
        <activity android:name=".activities.PrincipalDashboardActivity"/>
        <activity android:name=".activities.AddStudentActivity"/>
        <activity android:name=".activities.AddTeacherActivity"/>
        <activity android:name=".activities.ViewTeachersActivity"/>
        <activity android:name=".activities.UpdateTeacherActivity"/>
        <activity android:name=".activities.ViewStudentsActivity"/>
        <activity android:name=".activities.UpdateStudentActivity"/>


    </application>

</manifest>